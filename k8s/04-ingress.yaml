apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: himatsumu-ai-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: himatsumu-ai.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: himatsumu-ai-service
            port:
              number: 8000
